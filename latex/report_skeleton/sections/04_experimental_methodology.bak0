% ============================================================
% 4. Experimental Methodology
% ============================================================
\section{Experimental Methodology}
\label{sec:methodology}

\subsection{Datasets}
\label{sec:datasets}

\begin{table}[htbp]
    \centering
    \caption{Datasets Used in Experiments}
    \label{tab:datasets}
    \begin{tabular}{llcccc}
        \toprule
        \textbf{Name} & \textbf{Type} & \textbf{Entities} & \textbf{Overlap} & \textbf{Cardinality} & \textbf{Source} \\
        \midrule
        \texttt{smallestest} & Synthetic & 100 & 10\% & 100/120 & \texttt{generate\_data.py} \\
        \texttt{smaller} & Synthetic & 1,000 & 10\% & 100/120 & \texttt{generate\_data.py} \\
        \texttt{small} & Synthetic & 5,000 & 10\% & 100/120 & \texttt{generate\_data.py} \\
        \texttt{med\_30\_overlap} & Synthetic & 10,000 & 30\% & 100/120 & \texttt{generate\_data.py} \\
        \texttt{low\_card} & Synthetic & 100 & 10\% & 5/5 & \texttt{generate\_data.py} \\
        \texttt{c2} & Synthetic & 1,000 & 10\% & 200/200 & \texttt{generate\_data.py} \\
        \texttt{o50} & Synthetic & 1,000 & 50\% & 100/120 & \texttt{generate\_data.py} \\
        \texttt{DBLP-ACM} & Real (biblio) & ~2,600 & ~15\% & 1/1 & Public (DBLP/ACM) \\
        \texttt{MusicBrainz} & Real (music) & ~3,900 / ~3,800 & ~27\% & Multi / Multi & Public (MusicBrainz) \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Evaluation Protocol}
\label{sec:evaluation_protocol}

\subsubsection{Synthetic Data: Ground-Truth Available}
Since \texttt{latent\_event\_id} is preserved, we construct a clerical labels table:
\begin{enumerate}
    \item Merge predictions with ground truth on \texttt{latent\_event\_id\_l == latent\_event\_id\_r}.
    \item Compute TP, FP, FN, TN at each probability threshold.
    \item Derive Precision, Recall, $F_1$, Accuracy.
    \item Select optimal threshold via maximum $F_1$.
\end{enumerate}

\subsubsection{Public Data: No Ground Truth}
For DBLP-ACM and MusicBrainz, we rely on:
\begin{itemize}
    \item \textbf{Blocking recall bounds}: Theoretical upper bound on recall from blocking rule coverage.
    \item \textbf{Precision/Recall estimation}: Using the \splink{} accuracy analysis with a small manual clerical sample (if available) or synthetic proxy.
    \item \textbf{Stability checks}: Consistency of match weights across \emalgo{} rounds; separation of match/non-match weight distributions.
\end{itemize}

\subsubsection{Metrics}
\label{sec:metrics}
\begin{align}
    \text{Precision} &= \frac{TP}{TP + FP} \\
    \text{Recall} &= \frac{TP}{TP + FN} \\
    F_1 &= 2 \cdot \frac{\text{Precision} \cdot \text{Recall}}{\text{Precision} + \text{Recall}} \\
    \text{Accuracy} &= \frac{TP + TN}{TP + FP + FN + TN}
\end{align}

For global aggregation experiments, we also report \textbf{global entity-level} metrics after aggregation (matching on \texttt{global\_entity\_id}).

\subsection{Baselines and Ablations}
\label{sec:baselines}

\begin{description}
    \item[\textbf{Local Only (No Aggregation)}] Threshold local match probabilities directly; each local pair is a decision.
    \item[\textbf{Naive Sum Aggregation}] Sum probabilities per global pair; threshold.
    \item[\textbf{Normalised Aggregation}] Mean probability per global pair; threshold.
    \item[\textbf{Noisy-OR Aggregation}] Probabilistic union; threshold.
    \item[\textbf{Max Aggregation}] Max probability per global pair; threshold.
    \item[\textbf{Vector-Space Aggregation}] Profile vectors + second \splink{} layer (\Cref{sec:profile_aggregation}).
    \item[\textbf{Blocking Rule Variants}] Vary strictness/number of blocking rules.
    \item[\textbf{Target Recall Variants}] Vary initial recall for $\pi$ estimation (0.7, 0.9, 0.99).
    \item[\textbf{Data Config Variants}] \Cref{tab:configs}.
\end{description}

\subsection{Computational Environment}
\label{sec:compute_env}
\begin{itemize}
    \item DuckDB in-memory with 24GB memory limit, spill to disk.
    \item \splink{} 4.x (Python).
    \item Python 3.11, pandas, numpy, scikit-learn for metrics.
    \item Runtime: Local layer prediction ~1-10s depending on config; global aggregation negligible.
\end{itemize}